
# Definimos la variable opcion y la iniciamos en 0
opcion = 0

while opcion != 5:

    #Usamos la función print() para mostrar el menu
    print("\n**GESTOR DE NOTAS ACADÉMICAS**")
    print("1 Registrar un nuevo curso y nota")
    print("2 mostrar todas las notas registradas")
    print("3 Calcular el promedio general")
    print("4 eliminar un curso")
    print("5 Salir")

    try:
        opcion = int(input("Seleccione una opcion: "))
    except ValueError:
        print("Error** Por favor ingrese solo un número.")
        # Si hay un error, reiniciamos la opción y continuamos con lo que sigue
        # vuelta del bucle para volver a mostrar el menú.
        opcion = 0
        continue

    if opcion == 1:
        print("\n--- Has elegido registrar curso y nota ---")
        # solo es un marcador de posicion 
    elif opcion == 2:
        print("\n--- Has elegido mostrar todas las notas ---")        
    elif opcion == 3:
        print("\n--- Has elegido calcular el promedio general ---")        
    elif opcion == 4:
        print("\n--- Has elegido eliminar un curso ---")        
    elif opcion == 5:
        print("\nSaliendo del sistema...")
        
    else:
        #ste bloque se ejecuta si la opcion no es 1, 2, 3, 4 o 5
        print("\nOpcion no valida intente de nuevo.")

# Cuando el bucle 'while' termina (porque opcion es 5), el programa finaliza.
print("¡Hasta pronto!")
